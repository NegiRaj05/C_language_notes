#include <stdio.h>


int main(){

    float temp = 25;

    if(temp<=0 || temp>=30){
        printf("The weather is bad!");
    }
    else{
        printf("The weather is good!");
    }
    return 0;
}