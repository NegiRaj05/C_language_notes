# include <stdio.h>
# include <math.h>

int main(){

    // Math functions

    double a = sqrt(64);
    double b = pow(2,3);
    int c = round(3.49);
    int d = ceil(3.49);
    double e = fabs(-100);
    double f =log(3);
    double g = sin(45);
    double h = cos(30);


    //printf("%lf",f);

    
    return 0;
}